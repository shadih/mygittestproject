<settings>
  <!-- 
  If you prefer to relocate your local repository to somewhere other than ~.m2/repository
  then uncomment the <localrepository/> tag below and specify your location.
  <localRepository>c:/relocated/maven/repository</localRepository>
  -->
  <mirrors>
    <mirror>
      <id>nexus</id>
      <mirrorOf>*</mirrorOf>
      <url>http://mavencentral.it.att.com:8084/nexus/content/groups/att-public-group</url>
    </mirror>
  </mirrors>
  <profiles>
    <profile>
      <id>nexus</id>
      <!-- Enable snapshots for the built in central repo to direct -->
      <!-- all requests to nexus via the mirror -->
      <repositories>
        <repository>
          <id>central</id>
          <url>http://central</url>
          <releases>
            <enabled>true</enabled>
            <!--
            <updatePolicy>always</updatePolicy>
            <updatePolicy>never</updatePolicy>
            <updatePolicy>daily</updatePolicy>
            <updatePolicy>interval:in minutes</updatePolicy>
            -->
            <updatePolicy>never</updatePolicy>
          </releases>
          <snapshots>
            <enabled>true</enabled>
            <updatePolicy>always</updatePolicy>
            <!--
            <updatePolicy>always</updatePolicy>
            <updatePolicy>never</updatePolicy>
            <updatePolicy>daily</updatePolicy>
            <updatePolicy>interval:30</updatePolicy>
            -->
         </snapshots>
        </repository>
      </repositories>
     <pluginRepositories>
        <pluginRepository>
          <id>central</id>
          <url>http://central</url>
          <releases><enabled>true</enabled></releases>
          <snapshots><enabled>true</enabled></snapshots>
        </pluginRepository>
      </pluginRepositories>
    </profile>
  </profiles>
  <activeProfiles>
    <!-- make the profile active all the time -->
    <activeProfile>nexus</activeProfile>
  </activeProfiles>
  
  <pluginGroups>
    <!-- define the sonatype plugin group, so the nexus plugins will work without typing the groupId-->
  	<pluginGroup>org.sonatype.plugins</pluginGroup>
  </pluginGroups>
  <servers>
<server>
  <id>nexus</id>
  <username>TYQ/wqyU</username>
  <password>OgR4TTmWxL9spLWVWFd8hYrxLVSuAVfGe3FvzCauIDsT</password>
</server>
  </servers>
</settings>

<!-- For artifact deployment, Maven needs the distributionManagement element in the pom.xml file. -->
<!-- NOTE: DO NOT UNCOMMENT / expose the distributionManagement section below in your settings.xml file. -->
<!-- 
<distributionManagement>
  <repository>
    <id>nexus</id> 
    <name>att-repository-releases</name> 
    <url>http://mavencentral.it.att.com:8084/nexus/content/repositories/att-repository-releases</url> 
  </repository>
  <snapshotRepository>
    <id>nexus</id> 
    <name>att-repository-snapshots</name> 
    <url>http://mavencentral.it.att.com:8084/nexus/content/repositories/att-repository-snapshots</url> 
  </snapshotRepository>
</distributionManagement>
-->
